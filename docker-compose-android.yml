version: "3"
services:

  android:
    image: regis/openedx-android:${OPENEDX_RELEASE}
    build:
      context: ./android
    volumes:
      - ./config/android/:/openedx/config/
      - ./data/android/:/openedx/data
